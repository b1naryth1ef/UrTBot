EVENTS:
	api.registerEve('event', function)
	Should pull events from const (deaths) and allow for dynamic ones, so modules can do, api.registerHook('hook', **args) and then api.fireHook('hook')



- Kick/Banning
- Slapping/Nuking
- Team Killing
- Match Managment
- Database
- Threading


TODOS:
- Make sure RCON is workin [!!!!]
- Add a DB handler (either/both sqlite or mysql)
- Add basic commands, work on API
- Add lots of events
- etc

CMDS:
!set - Sets a server variable
!stop - Stop bot/server
!map - Switch to a map
